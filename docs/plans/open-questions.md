
## agentctl-claude-remote-control-hint - 2026-09-23
- [x] May a session's sanitized `name` appear in `use --live --json` warnings, or counts only? (U6) — decides D-041's --json half — DECIDED 2026-09-24 00:50 JST: counts only
- [x] Should the deferred automation include route A (reconnect only, loses claude.ai history)? (U5) — decides whether bead #4 has a fallback mode — DECIDED 2026-09-24 00:50 JST: A′ only
- [ ] Does a W4a namespace swap stop Remote Control without a .claude.json rewrite? (U1) — decides whether the namespace target needs the hint

## agentctl-claude-remote-control-hint v2 - 2026-09-24 00:10 JST
- [x] Confirm the U6 default: `--json` warnings carry counts only; sanitized names only in TTY sentences (D-041) — supersedes the 2026-09-23 U6 entry, which assumed names in `--json` — DECIDED 2026-09-24 00:51 JST by the user: counts only
- [x] Confirm the U5 default: automation is A′ only; route A, if ever added, is a separate explicit mode value, never default, never implied by `--yes` (D-043) — supersedes the 2026-09-23 U5 entry — DECIDED 2026-09-24 00:51 JST by the user: A′ only
- [ ] U1 widened: does an applied live swap whose config step reported `not_updated` (keychain changed, `.claude.json` not rewritten) stop Remote Control before the recovery re-run? — the plan now warns on every applied outcome regardless; bead #3's spike answers it

## agentctl-claude-remote-control-hint v5.1 - 2026-09-24 01:45 JST
- [x] U10: gate Phase 4 on an exact VERIFIED_VERSIONS allowlist (default; every Claude Code release disables the flag until an AC167 run adds it) or on a minimum verified version plus run-time field checks? — decides D-055 and how often the flag is off after an auto-update — DECIDED 2026-09-24 02:06 JST by the user: minimum verified version + runtime field checks (not the exact allowlist)
- [x] U11: keep D-051's all-or-nothing (default; one session that cannot be disconnected cancels the swap, exit 30) or proceed and report that session? — v5.1's step 12b-i makes the static-check case of exit 30 cost no disconnect or restore, but a flagged pass still cannot swap while one eligible session is unverifiable — DECIDED 2026-09-24 02:06 JST by the user: all-or-nothing (exit 30), the plan's default

## agentctl-claude-remote-control-hint v5.1.2 - 2026-09-24 02:11 JST
- [ ] AC145-time decision: if C29 finds the registry `version` carries no `major.minor.patch` triple, §17.4 check 2 skips every session and `--restart-remote-control` does nothing; decide then whether that is a §15.3 trigger (revise §17 to another version source) or a cancel condition.

## agctl-linux-support v1 - 2026-09-26 00:46 JST
- [x] U1: Does the Linux Claude Code build select the plaintext store for both legacy and V5 reads? (S4; settle on the Debian host against a matched Linux extraction, isolated synthetic store) — verdict: matched Linux 2.1.282 selects plaintext in both factories (`zn()` returns `In`; V5 `iC` wraps the file object; both use `<pw()>/.credentials.json`); isolated legacy reads measured, V5 selection source-established (docs/re-verify.md §7.2 U1, 2026-09-25 UTC).
- [x] U2: Linux precedence and writer states — v1.3 ruling 2026-09-26 11:05:02 JST (from `date`): D-060 retains agctl temp/fsync/rename; 0-byte and valid-keyless stores are Transient, never Absent, with AC190/AC195 same-inode and two-publication fixtures plus undetermined on observed post-write drift. The measured 2-byte publication's contents remain uninspected; no claim against a later write after return. D-068 refuses ambiguous Linux reads/status/writes with `ambiguous config dir: use an absolute path`, no cwd/home fallback; defined-empty secure-storage precedence and macOS behavior remain. S1 operator procedure complete, live exceptional fallback unmeasured. Critic APPROVE; S4 proves AC190/192/193/195 (docs/re-verify.md §7.2 U2, §7.4).
- [x] U3: Linux mutex/owner lifecycle — v1.3 ruling 2026-09-26 11:05:02 JST (from `date`): D-061 uses primary → canonical legacy → .storage-write.lock; owner metadata is not a lock/recovery authority, modes are umask-conditioned and heartbeat source-derived. D-066 recommends a separate user-approved cross-platform correction before Phase 2, retaining old .storage-write as a legacy agctl artefact only and forbidding platform-specific spelling. AC217 still unmeasured: separate fix supplies macOS proof; S4 requires real Debian contention plus a separately authorized throwaway forced-refresh operator checkpoint, not fake-peer substitution. Within-budget retry then success after release or refusal are valid, not mandatory onCompromised. S1 procedure is complete; critic APPROVE; user scope decision pending; no new bead authorized (docs/re-verify.md §7.2 U3, §7.4).
- [x] U4: Process visibility and identity — v1.3 ruling 2026-09-26 11:05:02 JST (from `date`): D-058 retains exact comm=claude and says `no stopped peer visible by exact name`; non-claude launchers/self-settable comm are residuals. kill-zero is hidepid-independent but PID-namespace scoped; nested view 3/496 does not prove same-UID hidepid loss or complete visibility of store-sharing peers. D-067 guards version/boot/ns/ticks/real UID before recovery; only same-boot/ns/UID plus kill ESRCH proves an own writer gone. Old/incomparable records remain unknown; writer death never proves ownership of a present path. Linux peer-lock removal always refuses, including doctor/attestation/--yes, while fresh own locks/flock work; SF1: Codex doctor reports unknown holder via namespace.lock = null + namespace.notes, schema unchanged. Critic APPROVE; S2 AC183–186 pending (docs/re-verify.md §7.2 U4/U7; lead analyst ruling).
- [x] U5: Can the installed Codex be proven to log in file-only on Linux despite managed/config overrides (auth_keyring.rs:71-73)? If not, the NO-GO is scoped to the Codex login feature on Linux (lead ruling R2), not to the plan. (S5, Phase 0) — verdict: no. The matched 0.157.0-alpha.10 binary with a private-mount managed requirement `keyring` defeats `-c file` argv (login/status exit 1, D-Bus attempted, no auth.json); D-064's `unsupported on this platform` before spawn is selected and accepted by the lead; proven file-backed Codex status/import/doctor retained (docs/re-verify.md §7.2 U5, 2026-09-25 UTC).
- [x] U6: Does the matched Linux Claude Code use /etc/claude-code as the managed-settings root (cli.unpack.js:47998@ac66a5f)? (S5) — verdict: yes; matched Linux source default and the isolated trace's O_PATH open/stat of /etc/claude-code/managed-settings.json agree (docs/re-verify.md §7.2 U6, 2026-09-25 UTC).
- [x] U7: Which procfs crate fits (license, maintenance), and are the Debian host toolchain, dev config and gate prerequisites intact? (S2/S3) — verdict: recommend procfs-core 0.18 (MIT OR Apache-2.0, defaults disabled) behind an agctl-owned bounded, validated input adapter; rustc/cargo 1.98.1 intact; strace/rg/tmux extracted from debs into /tmp tmpfs only (no apt install/sudo); cargo-nextest, direnv, dev config and a Git snapshot still required for Phase 1 (docs/re-verify.md §7.2 U7, host 2026-09-25 UTC, source review 2026-09-26 JST). v1.3 §7.1 reconciliation: the Debian gates run plain cargo with no `.envrc` and no inherited RUSTFLAGS (the macOS `+neon` flags are invalid on x86_64); nextest, rg and the dev config go into the tmpfs CARGO_HOME by the gate runner; direnv is not a Linux prerequisite.
- [x] U8: If proof-backed login is selected, does real Linux Codex file login work under scratch HOME/CODEX_HOME and scrubbed environment? (S5; operator handles browser step if needed) — verdict: not applicable; resolved by D-064's pre-spawn unsupported branch (accepted by the lead as the U5 outcome), so no Linux Codex OAuth child is enabled and no real browser login is needed or claimed; reopens only if a later reviewed plan enables login (docs/re-verify.md §7.2 U8, 2026-09-25 UTC).
- S1 verdicts applied by the lead 2026-09-26 10:16:14 JST (from `date`) from lane linux-s1's report; U2/U3 write-lifecycle sub-items and the U4 ruling stay open until the operator OAuth checkpoint (docs/re-verify.md §7.4) and the single planner + scoped-critic return. Operator OAuth checkpoint completed (real login, refresh, logout; scratch removed 2026-09-26 01:29:15 UTC); U2/U3 lines updated by the lead 2026-09-26 10:34:56 JST (from `date`); AC178/AC181 now blocked on the scoped review only.
- v1.3 return closed 2026-09-26 11:10:32 JST (from `date`): one planner pass (`planner-linux3`) + one scoped critic pass (`critic-linux3`) → APPROVE on SHA `7b79699…`; U2/U3/U4 lines above replaced with the final rulings; SF1 applied by the lead.
- [x] D-066 scope (user decision, before Phase 2): fix agctl's `.storage-write` → `.storage-write.lock` mismatch as a separate platform-independent bug bead landed and verified (macOS exclusion witness) before S4, or as an explicitly reviewed in-plan macOS carve-out. DECIDED 2026-09-26 11:42:30 JST (from `date`) by the user: separate platform-independent bug, filed as `agctl-storage-write-lock-name-4slm` (Claude storage mutex name mismatch, P1, independent of `agctl-1y9`, blocks `agctl-1y9.3`).
- [ ] AC217 Linux checkpoint (user authorization, Phase 2/S4): a separately authorized throwaway forced-refresh operator login on the Debian host to measure real agctl/peer contention on `.storage-write.lock`.
- [x] v1.3 confirmation and Phase 1 start order (user): Phase 1 (S2–S3) has structural GO; execution waits for the user's explicit order in a new session. — DECIDED 2026-09-26 11:42:30 JST (from `date`) by the user: v1.3 confirmed, Phase 1 GO; one executor lane S2→S3, one verifier lane AC182–AC189, six gates on both OSes at phase end, stop before S4.
